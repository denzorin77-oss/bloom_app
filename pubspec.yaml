name: bloom_app
description: "BLOOM - AI-советник по макияжу для подростков"
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  camera: ^0.10.5
  path_provider: ^2.1.2
  path: ^1.8.3
  permission_handler: ^11.3.1

flutter:
  uses-material-design: true
